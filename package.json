{
  "name": "firebase-supabase-function-bridge",
  "version": "2.0.0",
  "description": "Bridge between Firebase Functions and Supabase HTTP triggers.",
  "main": "index.js",
  "author": "Jared Williams <accounts@jaredwilliams.com.au>",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^1.22.3",
    "dotenv": "^10.0.0",
    "pg": "^8.7.1"
  },
  "peerDependencies": {
    "firebase-admin": "*",
    "firebase-functions": "*"
  },
  "devDependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0",
    "prettier": "^2.3.2",
    "typescript": "^5.3.2"
  },
  "bin": "bin/deploy",
  "scripts": {
    "prettier": "prettier \"src/**/*.js\" -w",
    "build": "rm -rf ./dist && tsc && cp package.json ./dist && cp -R ./bin ./dist"
  },
  "engines": {
    "node": ">=18"
  },
  "types": "index.d.ts"
}
